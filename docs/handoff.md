# Передача проекта между ПК и сессиями

## Минимальная команда для нового Codex

`Прочитай AGENTS.md, STATUS.md, PLAN.md и docs/variant.md. Проверь git status и продолжи с ближайшего незавершенного пункта, не запуская автоматизацию Multisim.`

## Протокол начала работы

Обновление 13.09.2026: пользователь собрал DC; сохранён `deliverables/multisim/DC.ms14`, обновлены остальные три модели и Word с новыми снимками. Не повторять сборку с нуля: сначала проверить текущие файлы. Проверка модели и новый постраничный рендер помощником ещё не выполнены; сведения ниже о несобранном DC относятся к предыдущему этапу.

Актуальное уточнение 06.09.2026: Word дописан до конца — 34 страницы, разделы 3.1–3.3, заключение и источники. Рисунки 2.8–2.23 сохранены, добавлены 3.1–3.2 в `deliverables/figures_final/`. Пользователь выбрал остановку на 6. Рисунок 2.11 использует один XLA; для 3.2 тот же прибор перенастраивается на внутреннюю выборку. Руководство сборки — `docs/multisim_22_23.md`. Новые блоки и объединённая модель Multisim ещё не проверены: следующий этап — ручная сборка и фактические снимки, а не повторное дописывание текста. Старые рисунки 2.14–2.16 в `figures_22_23` относятся к прежней версии, не брать их за актуальную нумерацию.

Скрипт `scripts/finish_coursework.py` рассчитан на 28-страничный исходник из коммита `7edfbe9` (`_docx_work/before_completion_20260906.docx` на этом ПК). Не запускать его поверх пользовательских правок. Проверка текущей версии: `scripts/check_completed_docx.py` после рендера в `_docx_work/completed_checked`; для проверки сохранности медиа нужен указанный исходник. PNG/PDF для QA в Git не включаются.

1. Убедиться, что открыта корневая папка репозитория.
2. Выполнить `git status --short --branch`.
3. Прочитать актуальный результат из `deliverables/` и статус.
4. Не трогать незакоммиченные пользовательские изменения.
5. Выбрать один ограниченный этап из `PLAN.md`.

## Протокол завершения этапа

1. Проверить расчеты или визуально проверить измененный документ.
2. Обновить `STATUS.md`: что закончено, что осталось, ближайший следующий шаг.
3. Добавить датированную запись в `CHANGELOG.md`.
4. Обновить чекбоксы в `PLAN.md`.
5. Проверить `git diff` и `git status`.
6. Создать коммит с понятным сообщением.

## Правило версий Word

- Текущая версия всегда находится по пути `deliverables/Курсовая_Блинов_текущая.docx`.
- Перед рискованной крупной переработкой можно создать архивную копию с датой, но не плодить версии после каждого мелкого исправления.
- После редактирования DOCX требуется полный постраничный рендер и визуальная проверка.

## Что передавать

Для другого ПК достаточно всего Git-репозитория. Временную папку `_docx_work/` переносить не нужно: она исключена из Git и может быть создана заново.
